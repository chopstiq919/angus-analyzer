{
  "name": "angus-breeding-analyzer",
  "version": "1.0.0",
  "description": "Black Angus seedstock breeding decision tool",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "postinstall": "node -e \"require('puppeteer')\" 2>/dev/null || npx puppeteer browsers install chrome"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.24.0",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "puppeteer": "^22.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
